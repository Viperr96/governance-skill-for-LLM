# Project

Always run `pytest` before you commit.
Never run `pytest` before you commit; it is too slow in this repo.
Format every file with `black` before committing.
Do not format with `black`; this repo uses `ruff format`.
